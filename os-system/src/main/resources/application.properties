spring.application.name=os-system

# ==============================================================
# 🗄️ CONFIGURAÇÃO DO BANCO DE DADOS (PostgreSQL)
# ==============================================================
# URL de conexão
spring.datasource.url=jdbc:postgresql://localhost:5432/controlsave_os

# ⚠️ CREDENCIAIS DO BANCO
spring.datasource.username=postgres
spring.datasource.password=!info159456@ti
spring.datasource.driver-class-name=org.postgresql.Driver

# ==============================================================
# 🧠 JPA / HIBERNATE (Gerenciamento de Tabelas)
# ==============================================================
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# ==============================================================
# 🌐 VARIÁVEIS DO SISTEMA (CORS)
# ==============================================================
# ✅ CORRIGIDO: Adicionado o seu IP real (192.168.0.11)
app.cors.origins=http://localhost:5173,http://192.168.0.11:5173